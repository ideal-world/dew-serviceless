{
  "name": "@idealworld/plugin",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "rimraf ./dist && lerna run clean",
    "build": "lerna run build",
    "deploy": "lerna run prePublish && lerna publish --skip-git",
    "test": "lerna run test"
  },
  "author": "gudaoxuri",
  "dependencies": {},
  "devDependencies": {
    "typescript": "^4.0.2",
    "lerna": "~3.22.1",
    "rimraf": "^3.0.2"
  }
}
