{
  "name": "@idealworld/plugin",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "lerna run clean",
    "build": "lerna run build",
    "deploy": "lerna publish",
    "local": "lerna publish --skip-git --skip-npm",
    "test": "lerna run test"
  },
  "author": "gudaoxuri",
  "dependencies": {},
  "devDependencies": {
    "typescript": "^4.0.2",
    "lerna": "~3.22.1"
  }
}
