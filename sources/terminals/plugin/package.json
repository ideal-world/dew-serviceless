{
  "name": "@dew/plugin",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b ./packages/tsconfig-pkg.json",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna run --parallel clean",
    "release": "lerna publish"
  },
  "author": "gudaoxuri",
  "dependencies": {},
  "devDependencies": {
    "lerna": "^3.22.1",
    "typescript": "^4.0.2"
  }
}
